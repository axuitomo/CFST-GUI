# AGENTS.MD

本文件定义本仓库中 AI 编码代理与协作者的工作规则。目标是让每次修改都简单、干净、可维护，并且优先复用仓库已有共享能力。

## 第一优先级

1. 保持正确性、稳定性和向后兼容。任何改动都不能破坏现有桌面端、WebUI、Android、CLI 或配置兼容行为，除非任务明确要求并说明迁移方式。
2. 优先 KISS / YAGNI。先做能解决当前真实问题的最小改动，不为假设需求提前设计复杂架构。
3. 优先复用共享包。通用逻辑优先放入或复用 `internal/*core`、`internal/sourceparse`、`internal/httpclient`、`frontend/src/lib` 等共享位置，其次才考虑业务模块内局部实现。
4. 修改代码之后必须考虑是否同步修改对应文档。涉及功能、配置、命令、接口、构建、平台行为变化时，应更新 `README.md` 或 `docs/` 下相关文档。
5. 不泄露敏感信息。不得提交 Token、密钥、私钥、凭据、真实用户数据或本地专有路径。

## 代码要求

代码应满足简易性、洁净性、易维护性和可测试性。

- 简易性：函数职责单一，控制流清晰，避免过深嵌套和隐式副作用。
- 洁净性：命名准确，删除无用代码，避免重复逻辑、魔法值和临时调试输出。
- 易维护性：遵循现有项目风格，不引入不必要的新框架、新目录或新抽象。
- 可测试性：核心逻辑尽量拆到可单测的 Go 包或前端工具函数中，避免只靠 UI 手动验证。
- 向后兼容：配置 schema、CLI 参数、API 返回结构、移动端桥接字段变更必须谨慎处理旧数据和旧调用方。

反例：

- 为单一页面需求复制一套已有解析、HTTP、归档或 DNS 逻辑。
- 为未来可能存在的需求提前添加复杂接口、插件系统或全局状态。
- 修改配置字段后不写兼容迁移，不更新文档，也不补测试。
- 在业务组件中硬编码可复用规则，导致桌面端、WebUI、Android 行为分叉。

正例：

- 把跨端共用逻辑沉淀到 `internal/probecore`、`internal/archivecore`、`internal/cloudflarecore`、`internal/sourceparse` 等共享包。
- 前端通用桥接、名称映射、输入源工具优先放入 `frontend/src/lib`。
- 新行为先补测试，再让 app 层、mobileapi 层或 UI 层调用同一个核心实现。
- 功能变化同时更新 `README.md`、`docs/index.md` 或对应专题文档。

## 决策方法

遇到不确定实现时，必须先做正反论和权重计算，再选择方案。权重不是形式主义，应服务于可维护决策。

推荐权重：

| 指标 | 权重 | 判断问题 |
| --- | ---: | --- |
| 正确性与兼容性 | 30 | 是否保留现有行为，是否能处理旧配置、旧平台和异常输入 |
| 共享复用程度 | 20 | 是否优先使用共享包，是否减少跨端重复实现 |
| 简易性 | 15 | 是否用较少概念解决问题，是否避免过度设计 |
| 可维护性 | 15 | 后续开发者是否容易理解、定位和修改 |
| 测试与验证 | 10 | 是否能用自动化测试或明确命令验证 |
| 文档同步 | 10 | 用户可见行为变化是否有对应说明 |

计算方式：

1. 给每个方案的每项指标打 0-5 分。
2. 使用 `总分 = Σ(指标分 / 5 * 权重)`。
3. 默认选择总分最高的方案。
4. 若低分方案更适合，必须写明选择理由和风险控制方式。

共享包优先规则：

- 若需求涉及跨端、跨模块或可复用业务规则，共享包方案的“共享复用程度”必须优先计高权重。
- 只有当共享抽象会明显增加复杂度、影响性能或污染核心包边界时，才允许选择局部实现。
- 局部实现若后续出现第二个调用点，应重新评估并抽取到共享包。

## 正反论模板

实施前对主要方案做简短正反论，尤其是跨文件、跨端、配置和接口变更。

```markdown
方案 A：复用或扩展共享包
正：减少重复；多端行为一致；测试集中。
反：可能需要调整包边界；短期改动面更大。
权重结论：若总分最高，优先采用。

方案 B：在当前模块局部实现
正：改动小；上线快；影响范围可控。
反：容易重复；后续多端行为可能分叉。
权重结论：仅适合一次性、强局部、无复用价值的逻辑。
```

## 苏格拉底提问法

编码前后用问题校准方向，不要机械执行。

1. 这个问题真实存在吗，还是只是在优化一个假设？
2. 最小可行改动是什么？
3. 现有共享包里是否已有相同或相近能力？
4. 如果把逻辑放在这里，三个月后谁最容易维护？
5. 这个改动会影响桌面端、WebUI、Android、CLI 或配置兼容吗？
6. 有没有反例输入、失败路径或旧数据会打破当前方案？
7. 用户文档、开发文档或发布说明是否需要同步？

## 仓库整理要求

仓库结构要条例分明，目录职责清晰，新增文件必须放在能表达职责的位置。

- Go 核心业务逻辑优先放入 `internal/*core` 或已有领域包。
- App 编排、Wails/WebUI 入口和平台适配逻辑放在 `internal/app`。
- Android Go bridge 能力放在 `mobileapi`，但应尽量复用 `internal` 中稳定核心逻辑。
- 前端通用工具放在 `frontend/src/lib`，组件和页面逻辑应避免承载跨端业务规则。
- 文档放在 `docs/`，快速入口和重要用户行为同步更新 `README.md`。

新增目录前必须确认：

1. 现有目录无法表达该职责。
2. 新目录不会与已有模块重叠。
3. 名称能让维护者一眼看出边界。
4. 有必要时补充文档索引或 README 说明。

## 修改流程

1. 先阅读相关代码、测试和文档，确认真实修改点。
2. 用正反论和权重计算选择实现方案；共享包优先。
3. 做最小必要修改，避免顺手重构无关代码。
4. 补充或更新测试，至少运行受影响范围的验证命令。
5. 检查文档是否需要同步；需要则同步更新。
6. 最后检查 `git diff`，确认没有敏感信息、无关格式化或生成物。

## 验证要求

能自动验证就自动验证。根据改动范围选择命令：

- Go 核心或后端改动：`go test ./...`
- 前端改动：在 `frontend/` 下运行 `npm run typecheck` 和必要的 `npm run build`
- 构建脚本或发行逻辑改动：运行对应脚本的最小目标，或说明无法完整运行的原因
- 文档改动：检查链接、命令、路径和版本描述是否与当前仓库一致

如果验证失败，必须记录失败原因、已尝试的修复和剩余风险。

## 文档同步规则

修改代码后必须主动判断是否需要修改对应文档。

单独修改业务文档、使用说明、部署文档、接口文档、发布说明等普通仓库文档时，不应顺带修改 `AGENTS.MD`。只有当仓库协作规范、代理规则或维护约定本身发生变化时，才允许修改 `AGENTS.MD`。

需要同步文档的情况：

- CLI 参数、运行方式、构建方式、环境变量变化。
- 配置字段、默认值、迁移兼容或储存路径变化。
- GUI/WebUI/Android 用户可见行为变化。
- API、事件、bridge、导入导出、DNS、WebDAV、GitHub 导出等接口变化。
- Release 产物、更新 manifest、平台支持范围变化。

不需要同步文档的情况也应能说明理由，例如内部重构且行为、命令、配置、接口完全不变。
