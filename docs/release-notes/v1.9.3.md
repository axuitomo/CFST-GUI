# CFST-GUI v1.9.3

## 变更摘要

- 修复 Go 间接依赖版本并刷新前端锁文件。
- 将受公开安全公告影响的 `tar`、`postcss`、`nanoid` 和 `brace-expansion` 依赖锁定到修复版本。
- 更新 Playwright 与 Node.js 类型定义依赖。
- 发布流水线现在强制校验 Windows amd64 EXE 和 Android arm64-v8a APK 均存在并匹配发布版本。
- 更新发布清单生成逻辑：1.9.3 的 Windows 与 Android 资产可独立生成，Linux 资产仅在实际存在时加入清单。

## 安全审计

- `pnpm audit --registry=https://registry.npmjs.org`：0 个漏洞。
- 重点修复公开公告 GHSA-23hp-3jrh-7fpw、GHSA-8x88-c5mf-7j5w、GHSA-r292-9mhp-454m、GHSA-r28c-9q8g-f849、GHSA-28wg-ghj8-5hjv 和 GHSA-rgw5-rvv9-x895。

## 验证

- `pnpm install --frozen-lockfile --registry=https://registry.npmjs.org`：通过。
- `pnpm typecheck`：通过。
- `pnpm test`：3 个测试通过。
- `pnpm build`：通过。
- `go test ./...`：受网络无法下载更新后的 Go 模块及缺失 `go.sum` 条目影响，未完全通过。
