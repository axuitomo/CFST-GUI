# CFST-GUI v1.9.2

## 变更摘要

- 优化 MCIS 候选结果复用与探测流程，减少重复 TCP 测量并保留 MCIS 诊断数据。
- 完善输入源、阶段工作流、结果处理和配置快照的兼容性与测试覆盖。
- 调整默认配置：测速后 Cloudflare 推送默认启用，GitHub 导出仓库信息默认留空。
- 发布版本统一提升到 `1.9.2`，Android 默认 `versionCode` 提升到 `10902`。
- 发布流水线仅上传 Windows、Android 和 update manifest 资产，不发布 Linux 或 Docker 资产。

## 验证

- `git diff --check`
- `go test ./...`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm build`
- `bash scripts/release-preflight.sh 1.9.2 --allow-dirty`

## 发行资产

| 平台 | 资产 |
| --- | --- |
| Windows | `cfst-gui-windows-amd64.exe` |
| Android arm64-v8a | `cfst-gui-android-arm64-v8a-release.apk` |
| Update manifest | `cfst-gui-update-manifest.json` |
