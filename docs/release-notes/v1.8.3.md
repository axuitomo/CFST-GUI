# CFST-GUI v1.8.3

## 变更摘要

- 前端工具链同步到 Vite 8、Tailwind CSS 4、TypeScript 6、`@vitejs/plugin-vue` 6 和 `vue-tsc` 3；Tailwind 改由 `@tailwindcss/vite` 接入，PostCSS 配置只保留 Autoprefixer，并刷新 `frontend/dist` hashed assets。
- 桌面端和移动端布局继续收口：主壳、Dashboard、DNS、Workflow、Results 和 Pipeline Studio 视图统一使用新的桥接值处理与响应式布局细节。
- Pipeline Studio 目录、节点、检查器和运行结果展示更新，改进运行状态、节点配置和结果字段的前端处理。
- Cloudflare DNS、colo 字典、测速结果、端口来源、CSV 输出和上传选择链路补充测试与边界处理。
- WebUI、桥接配置、更新检查和命令返回结构继续对齐三端行为，减少桌面、WebUI 和 Android 的差异。
- Android Activity 保持 edge-to-edge WebView 布局但不再隐藏状态栏/导航栏，Android P+ 启用 display cutout 短边布局以兼容刘海屏、打孔屏等异形屏。
- Android 原生层关闭 theme force dark、WebView `FORCE_DARK_OFF` 和 Android 13+ algorithmic darkening，避免按钮背景被系统/WebView 自动变淡、按钮文字被改成低对比颜色。
- Android 输入框聚焦时不再用 `visualViewport` 高度驱动 app 根容器，也不再强制 `scrollIntoView` 居中滚动；前端只同步键盘 inset 与键盘开闭状态，减少安装确认页返回后点击输入框触发的 WebView 重排抖动。
- `scripts/android-doctor.sh` 与 `scripts/release-preflight.sh` 新增 Android system bars 可见、display cutout 布局、禁止隐藏状态栏、禁止 WebView 自动暗化和禁止输入聚焦强制居中滚动的阻塞检查。
- GitHub Release、Android resubmit、GHCR / Docker、Wails 和 Android 默认版本号统一提升到 `1.8.3`，Android 默认 `versionCode` 提升到 `10803`。

## 验证

- `bash scripts/release-preflight.sh 1.8.3 --allow-dirty`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `go test ./...`
- `go test -tags tray ./...`
- `bash scripts/docs-check.sh`
- `git diff --check`
- `bash scripts/android-doctor.sh`
- Android 真机/AVD 手测：在线更新下载、APK 安装确认页返回、点击输入框不闪烁、状态栏保持可见、刘海屏/打孔屏 safe-area 避让正常
- `mobile/android/gradlew testDebugUnitTest`
- `mobile/android/gradlew lintDebug`
- `mobile/android/gradlew assembleDebug`

## 发行资产

| 平台 | 资产 |
| --- | --- |
| Windows | `cfst-gui-windows-amd64.exe` |
| Linux amd64 | `cfst-gui-linux-amd64.tar.gz` |
| Linux arm64 | `cfst-gui-linux-arm64.tar.gz` |
| macOS amd64 | `cfst-gui-darwin-amd64.app.zip` |
| macOS arm64 | `cfst-gui-darwin-arm64.app.zip` |
| Android universal | `cfst-gui-android-release.apk` |
| Android arm64-v8a | `cfst-gui-android-arm64-v8a-release.apk` |
| Android armeabi-v7a | `cfst-gui-android-armeabi-v7a-release.apk` |
| Update manifest | `cfst-gui-update-manifest.json` |
| GHCR multi-arch | `ghcr.io/axuitomo/cfst-gui:1.8.3`、`ghcr.io/axuitomo/cfst-gui:v1.8.3` |
